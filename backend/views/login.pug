extends layout

block logout

block content
  form(id="login-form" action="/login" method="post")
    labe(for="user") username:
    input#message(type="text" name="user")
    br
    labe(for="pass") Password:
    input#message(type="password" name="pass")
    br
    input(type="submit" value="Login")

block chat
    